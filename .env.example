# Copy to .env and fill in from Supabase project settings
SUPABASE_URL=
SUPABASE_POSTGRES_URL=

# Twilio SMS integration
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Google API keys for AI agent
GOOGLE_API_KEY=
GOOGLE_MAPS_API_KEY=

# External workflow bridge base URL (optional - now using AI agent directly)
# The API forwards inbound SMS events to: ${WORKFLOW_WEBHOOK_URL}/sms
# WORKFLOW_WEBHOOK_URL=

# Optional: Twilio voice Media Stream. In Twilio dashboard set "A call comes in" to:
#   https://your-api.example.com/twilio/webhooks/voice
# (no /realtime in the webhook URL). This env is the WebSocket stream URL returned in TwiML:
# Default pipeline: wss://your-api.example.com/ws/voice
# Realtime (OpenAI): wss://your-api.example.com/ws/voice/realtime
# VOICE_STREAM_WS_URL=

# Optional: OpenAI API key for voice transcription (Whisper). If set, speech before 2s silence is transcribed and logged.
# OPENAI_API_KEY=

# Optional: ElevenLabs API key for TTS (AI voice reply on calls). If set, transcript is spoken back over the call.
# For v3 hyper-realistic voice set ELEVEN_LABS_MODEL_ID=eleven_v3 (or eleven_multilingual_v2 as default).
# ELEVEN_LABS_API_KEY=
# ELEVEN_LABS_MODEL_ID=eleven_v3

# Production: set VITE_API_URL in Vercel (Frontend env vars) to your API origin
# so the built app calls the real API (e.g. https://hack-europe.vercel.app)
# VITE_API_URL=

# Frontend map rendering (Mapbox GL)
# Generate at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=
